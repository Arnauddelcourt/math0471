
FILE(GLOB SRCS *.h *.cpp)
ADD_LIBRARY(vtklite SHARED ${SRCS})

FIND_PACKAGE(ZLIB)
IF(ZLIB_FOUND)
    add_definitions(-DUSE_ZLIB)
    INCLUDE_DIRECTORIES( ${ZLIB_INCLUDE_DIR} )
    TARGET_LINK_LIBRARIES(vtklite ${ZLIB_LIBRARY} )
ENDIF()

