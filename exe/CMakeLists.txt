
SET(SRCS1 cube.h cube.cpp)

ADD_EXECUTABLE(sph ${SRCS1} sph.cpp)
ADD_EXECUTABLE(fdtd ${SRCS1} fdtd.cpp)

INCLUDE_DIRECTORIES( ${PROJECT_SOURCE_DIR}/src )

TARGET_LINK_LIBRARIES(sph vtklite)
TARGET_LINK_LIBRARIES(fdtd vtklite)
